'use client'

import styled from 'styled-components'

export const Label = styled.span`
  display: inline-flex;
  align-items: center;
  img {
    margin-right: 0.2rem;
  }
  background: var(--lighterColor);
  padding: 0 0.3rem;
  border: 2px solid var(--lighterColor0);
  border-radius: 0.3rem;
  span {
    margin-top: 0.2rem;
  }
`
