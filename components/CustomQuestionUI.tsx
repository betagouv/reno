import styled from 'styled-components'

export const CustomQuestionWrapper = styled.section`
  margin-top: 0.6rem;
  h2 {
    margin-bottom: 2vh;
  }
  header {
    small {
      color: var(--color);
      font-weight: 500;
    }
    h2 {
      margin-top: 0;
    }
  }
`
