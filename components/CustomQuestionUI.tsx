import styled from 'styled-components'

export const CustomQuestionWrapper = styled.section`
  margin: 0.6rem 1rem;
  h2 {
    margin-bottom: 2vh;
  }
  header {
    small {
      color: var(--color);
      font-weight: 500;
      font-size: 100%;
    }
    h2 {
      margin-top: 0.4rem;
    }
  }
`
