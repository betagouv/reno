import styled from 'styled-components'

export const CustomQuestionWrapper = styled.section`
  clear: both;
  margin-top: 0.6rem;
  h2 {
    margin-bottom: 2vh;
  }
  header {
    small {
      color: var(--color);
      font-weight: 500;
      font-size: 100%;
    }
    h2 {
      margin-top: 0.4rem;
    }
  }
`
