'use client'

import styled from 'styled-components'

export const Fieldset = styled.fieldset`
  li {
    display: flex;
    flex-wrap: wrap;
    flex-direction: column;
    label {
      margin: 0 1rem;
      whitespace: nowrap;
    }
  }
`
